import { EditorschoicePipe } from './editorschoice.pipe';

describe('EditorschoicePipe', () => {
  it('create an instance', () => {
    const pipe = new EditorschoicePipe();
    expect(pipe).toBeTruthy();
  });
});
